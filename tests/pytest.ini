[pytest]
# 临时 scratch 测试文件（_tmp_*.py）经常写完即删，避免 pytest 收集进测试套件
addopts = --ignore-glob='*_tmp_*.py' --ignore-glob='_tmp_*'
testpaths = tests
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*
